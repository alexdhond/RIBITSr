% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ribits-user.R
\name{rb_get_data}
\alias{rb_get_data}
\title{Get RIBITS Data (StreamCat-style Wrapper)}
\usage{
rb_get_data(
  aoi = NULL,
  type = "banks",
  x_coord = NULL,
  y_coord = NULL,
  state = NULL,
  ...
)
}
\arguments{
\item{aoi}{Area of Interest. Can be:
\itemize{
\item State abbreviation (e.g., "CA", "OR")
\item USACE District name (e.g., "Portland", "Sacramento")
\item Numeric Bank ID (e.g., 17)
}}

\item{type}{Type of data: "banks" (default), "ilf", "umbrellas"}

\item{x_coord}{Optional longitude (if searching by location - future feature)}

\item{y_coord}{Optional latitude (if searching by location - future feature)}

\item{state}{Optional state filter (explicit)}

\item{...}{Additional arguments passed to \code{ribits()}}
}
\value{
A \code{ribits_data} object
}
\description{
A simplified, text-based interface for retrieving data, designed to feel familiar
to users of \code{StreamCatTools}. Uses a generic \code{aoi} (Area of Interest) argument
to intelligently filter data.
}
\examples{
\dontrun{
# Get data for California (aoi = state)
ca <- rb_get_data(aoi = "CA")

# Get data for Portland District (aoi = district)
pd <- rb_get_data(aoi = "Portland")

# Get specific bank (aoi = ID)
bank <- rb_get_data(aoi = 17)
}
}
