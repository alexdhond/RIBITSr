% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-sources.R, R/harmonization-report.R
\name{rb_compare_sources}
\alias{rb_compare_sources}
\title{Compare ledger data completeness across sources (Internal)}
\usage{
rb_compare_sources(data, bank_id = NULL, field = NULL)

rb_compare_sources(data, bank_id = NULL, field = NULL)
}
\arguments{
\item{data}{A ribits_data object}

\item{bank_id}{Optional bank ID to filter comparison. If NULL, shows all banks
with discrepancies.}

\item{field}{Optional field name to filter comparison. If NULL, shows all fields
with discrepancies.}

\item{verbose}{Print detailed output. Default TRUE.}
}
\value{
A list with completeness metrics (invisibly)

Invisibly returns a tibble with the comparison data
}
\description{
This function is internal. Use \code{rb_diagnose(data, type = "sources")} instead.

Compares data completeness across sources (API, CSV, merged) to help
understand which source provides more complete data.

View side-by-side comparison of all data sources (API, CSV, EPA) for specific
banks or fields. This helps understand why sources disagree and what the
auto-harmonization logic chose.
}
\examples{
\dontrun{
# Recommended: Use rb_diagnose() instead
wy <- ribits(state = "WY")
rb_diagnose(wy, type = "sources")

# Internal usage
rb_compare_sources(wy)
}
\dontrun{
ca <- ribits(state = "CA")

# Compare all sources for a specific bank
rb_compare_sources(ca, bank_id = "SAC-001")

# Compare a specific field across all banks
rb_compare_sources(ca, field = "total_credits")

# Focused comparison: one bank, one field
rb_compare_sources(ca, bank_id = "SAC-001", field = "total_credits")
}
}
\keyword{internal}
