% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ribits.R
\name{ribits}
\alias{ribits}
\title{Get RIBITS data (auto-harmonized from all sources)}
\usage{
ribits(
  bank_ids = NULL,
  state = NULL,
  district = NULL,
  what = "all",
  type = "banks",
  sources = c("api", "epa", "csv"),
  cache = TRUE,
  quietly = FALSE
)
}
\arguments{
\item{bank_ids}{Optional vector of bank IDs. If NULL, fetches all banks
matching the filter criteria.}

\item{state}{Optional state filter (e.g., "CA", "TX")}

\item{district}{Optional USACE district filter}

\item{what}{What data to return. Options:
\itemize{
\item "all" (default): banks, ledger, footprints, service_areas
\item "banks": just bank summary data
\item "ledger": transaction/credit data
\item "spatial": footprints and service areas
}}

\item{type}{Type of resource to fetch:
\itemize{
\item "banks" (default): mitigation banks
\item "ilf": In-Lieu Fee programs
\item "umbrellas": Umbrella instruments
}}

\item{sources}{Which data sources to use:
\itemize{
\item c("api", "epa", "csv") (default): all sources
\item "api": RIBITS API only
\item "epa": EPA ArcGIS only
\item "csv": Direct CSV downloads only
}}

\item{cache}{Cache downloaded CSV files? Default TRUE.}

\item{quietly}{Suppress progress messages. Default FALSE.}
}
\value{
A \code{ribits_data} object containing harmonized data with:
\item{banks}{Bank summary tibble}
\item{ledger}{Transaction/ledger tibble}
\item{footprints}{sf object with footprint polygons}
\item{service_areas}{sf object with service area polygons}
\item{.meta}{Metadata including sources used and any discrepancies}
}
\description{
The main entry point for fetching RIBITS data. Automatically queries
all available sources (RIBITS API, EPA ArcGIS, direct CSV downloads),
harmonizes the data, detects discrepancies, and returns the best available data.
}
\details{
\strong{Note:} Most users should use the simpler wrapper functions instead:
\itemize{
\item \code{rb_banks()} - Get bank data
\item \code{rb_ilf_programs()} - Get ILF program data
\item \code{rb_umbrellas()} - Get umbrella instrument data
\item \code{rb_credits()} - Get credit tracking/classification data
}
}
\examples{
\dontrun{
# Recommended: Use simple wrapper functions
ca <- rb_banks(state = "CA")

# Advanced: Direct ribits() usage
ca <- ribits(state = "CA")

# Access the data
ca$banks
ca$ledger
ca$footprints

# Check for data quality issues
ca$.meta$discrepancies

# Get specific banks
data <- ribits(bank_ids = c(17, 100, 345))

# Just get spatial data
spatial <- ribits(state = "OR", what = "spatial")

# Control data sources
api_only <- ribits(state = "TX", sources = "api")
}
}
\keyword{internal}
