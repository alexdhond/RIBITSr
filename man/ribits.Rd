% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ribits-user.R
\name{ribits}
\alias{ribits}
\title{Get RIBITS Data}
\usage{
ribits(
  type = "banks",
  state = NULL,
  district = NULL,
  ids = NULL,
  transactions = c("comprehensive", "basic", "none"),
  spatial = TRUE,
  sources = c("api", "epa", "csv"),
  cache = TRUE,
  include_summaries = TRUE,
  quietly = FALSE,
  id = NULL
)
}
\arguments{
\item{type}{Type of data to fetch:
\itemize{
\item "banks" (default): Mitigation banks
\item "ilf": In-Lieu Fee programs
\item "umbrellas": Umbrella mitigation instruments
}}

\item{state}{State filter (e.g., "CA", "OR", "TX"). Full names also accepted.}

\item{district}{USACE district filter (e.g., "Portland", "Sacramento")}

\item{ids}{Optional vector of specific IDs to retrieve (bank_ids, program_ids, etc.).
Alias: \code{id} can also be used.}

\item{transactions}{Transaction detail level (banks only). Default "comprehensive".
\itemize{
\item "comprehensive": All transaction data (~85 columns) - RECOMMENDED
\item "basic": API ledger only (~20 columns, faster)
\item "none": No transaction data
}}

\item{spatial}{Include spatial geometries? Default TRUE.}

\item{sources}{Data sources to use. Default c("api", "epa", "csv") - all sources.
Using all sources provides the most complete data. If one fails, others are used.}

\item{cache}{Cache downloaded files? Default TRUE. Speeds up repeated queries.}

\item{include_summaries}{Include summary columns? Default TRUE. Adds transaction
volume, temporal patterns, geography, and public notice counts.}

\item{quietly}{Suppress progress messages? Default FALSE.}

\item{id}{Alternative for \code{ids} (for backward compatibility).}
}
\value{
A \code{ribits_data} object containing:
\item{banks}{Summary data (tibble) - always included}
\item{transactions}{Transaction/credit tracking data (tibble, if detailed=TRUE)}
\item{credits}{Credit classifications by type (tibble, if detailed=TRUE)}
\item{notices}{Public notices (tibble, if detailed=TRUE)}
\item{geometry}{Spatial data including footprints and service areas (sf object)}
\item{.meta}{Metadata including sources used and any discrepancies}
}
\description{
The main function for downloading mitigation banking data. Automatically fetches
and harmonizes data from all available sources (RIBITS API, EPA ArcGIS, and
CSV reports). By default, retrieves the maximum amount of data with all columns.

\strong{Philosophy:} Get everything, then filter using tidyverse. This function gives
you all the data; use dplyr/tidyr to shape it for your analysis.
}
\examples{
\dontrun{
# The simple way - get everything for California
ca <- ribits(state = "CA")

# Access the data
ca$banks           # All bank attributes, all sources merged
ca$transactions    # All transaction data (~85 columns)
ca$credits         # Credit classifications (NEW)
ca$notices         # Public notices (NEW)
ca$geometry        # Spatial footprints and service areas

# Filter with tidyverse
library(dplyr)
ca$banks \%>\%
  filter(bank_status == "Approved") \%>\%
  select(bank_id, bank_name, total_acres, available_credits)

# Get ILF programs
ilf <- ribits(type = "ilf", state = "TX")

# Get specific banks by ID
my_banks <- ribits(ids = c(17, 100, 345))
}
}
