% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{rb_config}
\alias{rb_config}
\title{Configure RIBITSr package settings}
\usage{
rb_config(
  max_retries = NULL,
  retry_delay = NULL,
  backoff_multiplier = NULL,
  timeout = NULL,
  rate_limit = NULL,
  checkpoint_dir = NULL,
  verbose = NULL,
  use_persistent_cache = NULL,
  cache_max_age_days = NULL,
  custom_cache_dir = NULL,
  source_priority = NULL,
  auto_resolve = NULL,
  reset = FALSE
)
}
\arguments{
\item{max_retries}{Maximum number of retry attempts for failed requests.
Default: 3}

\item{retry_delay}{Initial delay between retries in seconds. Increases
exponentially with backoff multiplier. Default: 2}

\item{backoff_multiplier}{Exponential backoff multiplier for retry delays.
Default: 2 (delays: 2s, 4s, 8s, ...)}

\item{timeout}{Request timeout in seconds. Default: 30}

\item{rate_limit}{Maximum requests per second. Set to NULL for unlimited.
Default: 5}

\item{checkpoint_dir}{Directory for saving operation checkpoints. NULL uses
tempdir(). Set to FALSE to disable. Default: NULL}

\item{verbose}{Show detailed progress messages. Default: TRUE}

\item{use_persistent_cache}{Use persistent cache across R sessions (stored
in user cache directory). FALSE uses session-only temp cache. Default: FALSE}

\item{cache_max_age_days}{Days before cached data expires and is re-downloaded.
Default: 30}

\item{custom_cache_dir}{Custom cache directory path. NULL uses default
(rappdirs::user_cache_dir for persistent, tempdir for session). Default: NULL}

\item{source_priority}{Character vector specifying data source priority
for conflict resolution: "api", "csv", "epa". Default: c("csv", "api", "epa")}

\item{auto_resolve}{Automatically resolve data conflicts using priority rules.
Default: TRUE}

\item{reset}{Reset all settings to defaults. Default: FALSE}
}
\value{
Invisibly returns current configuration as a list. When called without
arguments, prints formatted configuration and returns invisibly.
}
\description{
Unified configuration function for all RIBITSr settings including network
behavior, caching, rate limiting, and data harmonization preferences.

Call without arguments to view current configuration. Call with parameters
to update settings. Settings persist for the R session and can be overridden
with environment variables (see Details).
}
\details{
\subsection{Environment Variables}{

Configuration can be set via environment variables (read on package load):
\itemize{
\item \code{RIBITS_MAX_RETRIES}: Maximum retry attempts
\item \code{RIBITS_RETRY_DELAY}: Initial retry delay (seconds)
\item \code{RIBITS_TIMEOUT}: Request timeout (seconds)
\item \code{RIBITS_RATE_LIMIT}: Max requests per second
\item \code{RIBITS_USE_PERSISTENT_CACHE}: Enable persistent cache (true/false)
\item \code{RIBITS_CACHE_MAX_AGE_DAYS}: Cache expiry (days)
\item \code{RIBITS_CACHE_DIR}: Custom cache directory path
\item \code{RIBITS_VERBOSE}: Show verbose messages (true/false)
}

Environment variables are loaded once when the package is loaded. Use
\code{rb_config()} to change settings during the session.
}

\subsection{Cache Behavior}{
\itemize{
\item \strong{Session cache} (default): Fast, cleared when R session ends. Good for
interactive analysis.
\item \strong{Persistent cache}: Survives R restarts. Good for reproducible workflows
and reducing API load. Enable with \code{use_persistent_cache = TRUE}.
}

Clear cache with \code{rb_clear_cache()}.
}
}
\examples{
# View current settings
rb_config()

# Most common: Enable persistent caching
rb_config(
  use_persistent_cache = TRUE,   # Cache survives R restart
  cache_max_age_days = 7          # Refresh weekly
)

# For slow/unreliable networks
rb_config(
  max_retries = 5,      # More retry attempts
  timeout = 120,        # Longer timeout
  rate_limit = 2        # Slower rate (2 req/sec)
)

# For maximum speed (careful with API limits!)
rb_config(rate_limit = NULL)

# Reset to defaults
rb_config(reset = TRUE)

# Typical usage - just set persistent cache once:
rb_config(use_persistent_cache = TRUE)
ca <- ribits(state = "CA")  # Downloads and caches
ca <- ribits(state = "CA")  # Uses cache (instant!)
}
