% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-harmonize.R
\name{rb_get_geometry}
\alias{rb_get_geometry}
\title{Get bank geometry from best available source}
\usage{
rb_get_geometry(
  bank_ids,
  geometry_type = c("centroid", "footprint", "service_area", "all"),
  prefer_epa = TRUE
)
}
\arguments{
\item{bank_ids}{Vector of bank IDs to fetch geometry for}

\item{geometry_type}{Type of geometry to retrieve:
\itemize{
\item "centroid": Bank location point (fastest)
\item "footprint": Bank footprint polygon
\item "service_area": Bank service area polygon
\item "all": All available geometry types (returns list-columns)
}}

\item{prefer_epa}{Logical. If TRUE (default), try EPA ArcGIS first for
approved banks. Set to FALSE to always use API.}
}
\value{
An sf object with bank geometries and metadata:
\describe{
\item{bank_id}{Bank identifier}
\item{bank_name}{Bank name (if available)}
\item{geometry_source}{Source of geometry: "epa" or "api"}
\item{epa_available}{Whether EPA had geometry for this bank}
\item{status_limitation}{Note if geometry limited by bank status}
\item{geometry}{The spatial geometry}
}
}
\description{
Retrieves spatial data for mitigation banks from EPA ArcGIS (preferred for
approved banks) or RIBITS API (for non-approved banks or when EPA unavailable).
This provides a unified interface that automatically selects the best source
based on bank status and data availability.
}
\details{
\subsection{Data Source Behavior}{

\strong{EPA ArcGIS (prefer_epa = TRUE)}
\itemize{
\item Returns geometry for Approved banks only
\item Faster for bulk queries (single request)
\item Has layers: approved_banks (centroids), bank_footprints, bank_service_areas
}

\strong{RIBITS API (prefer_epa = FALSE or non-approved banks)}
\itemize{
\item Returns geometry for all bank statuses
\item Requires individual bank queries (slower for bulk)
\item Extracts from nested GeoJSON fields
}
}
}
\examples{
\dontrun{
# Get footprints for specific banks
footprints <- rb_get_geometry(c(3646, 3647), geometry_type = "footprint")

# Get service areas, always using API
service_areas <- rb_get_geometry(c(3646), geometry_type = "service_area",
                                  prefer_epa = FALSE)

# Get all geometry types
all_geom <- rb_get_geometry(c(3646), geometry_type = "all")
}
}
\keyword{internal}
