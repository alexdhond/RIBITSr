% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple-api.R
\name{rb_banks}
\alias{rb_banks}
\title{Get all bank data (fully harmonized)}
\usage{
rb_banks(
  state = NULL,
  district = NULL,
  bank_ids = NULL,
  ledger = TRUE,
  spatial = TRUE,
  sources = c("api", "epa", "csv"),
  cache = TRUE,
  quietly = FALSE
)
}
\arguments{
\item{state}{State filter (e.g., "CA", "OR", "TX")}

\item{district}{USACE district filter (e.g., "Portland", "Sacramento")}

\item{bank_ids}{Optional vector of specific bank IDs to retrieve}

\item{ledger}{Include transaction/credit ledger data? Default TRUE.}

\item{spatial}{Include footprint and service area geometries? Default TRUE.}

\item{sources}{Which data sources to use. Default: all available
\itemize{
\item "api" = RIBITS API only (fast, real-time)
\item "epa" = EPA ArcGIS only (spatial data)
\item "csv" = Direct CSV downloads (official records)
\item c("api", "epa", "csv") = All sources (recommended, default)
}}

\item{cache}{Cache downloaded CSV files? Default TRUE. Files cached in
session temp directory and reused if called again.}

\item{quietly}{Suppress progress messages? Default FALSE.}
}
\value{
A \code{ribits_data} object containing:
\item{banks}{Bank summary data (tibble)}
\item{ledger}{Transaction/credit tracking data (tibble)}
\item{footprints}{Bank footprint geometries (sf object)}
\item{service_areas}{Service area geometries (sf object)}
\item{.meta}{Metadata including sources used and any discrepancies}
}
\description{
The simplest way to get mitigation bank data. Automatically fetches and
harmonizes data from all available sources:
\itemize{
\item RIBITS API (real-time data)
\item EPA ArcGIS MapServer (spatial data)
\item RIBITS CSV reports (official records, fetched directly)
}
}
\details{
No manual downloads required - everything is automatic!
}
\examples{
\dontrun{
# Get all California banks (fully harmonized, automatic!)
ca_banks <- rb_banks(state = "CA")

# Access the data
ca_banks$banks           # Summary data
ca_banks$ledger          # Transaction history
ca_banks$footprints      # Spatial polygons

# Check data quality
ca_banks$.meta$discrepancies  # Any conflicts between sources?

# Get specific banks by ID
my_banks <- rb_banks(bank_ids = c(17, 100, 345))

# Just summary data, no spatial (faster)
summary <- rb_banks(state = "OR", spatial = FALSE)

# Only use API source (fastest, real-time)
api_only <- rb_banks(state = "TX", sources = "api")

# Export to CSV for analysis
readr::write_csv(ca_banks$banks, "california_banks.csv")
}
}
