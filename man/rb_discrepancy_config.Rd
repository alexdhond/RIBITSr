% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization-config.R
\name{rb_discrepancy_config}
\alias{rb_discrepancy_config}
\title{Configure discrepancy resolution priorities}
\usage{
rb_discrepancy_config(
  source_priority = c("csv", "api", "epa"),
  numeric_tolerance = 0.01,
  date_tolerance_days = 0,
  string_matching = c("exact", "ignore_case", "fuzzy"),
  flag_threshold = 5,
  auto_resolve = TRUE,
  auto_harmonize = TRUE
)
}
\arguments{
\item{source_priority}{Character vector specifying source priority order:
\itemize{
\item "api" = RIBITS API (default first - most current)
\item "csv" = RIBITS CSV reports (most complete/official)
\item "epa" = EPA ArcGIS (best spatial coverage)
}}

\item{numeric_tolerance}{Numeric tolerance for considering values "equal".
Default 0.01 (1\% difference considered equal).}

\item{date_tolerance_days}{For date fields, days difference considered equal.
Default 0 (exact match required).}

\item{string_matching}{How to compare strings:
\itemize{
\item "exact" (default): case-sensitive exact match
\item "ignore_case": case-insensitive
\item "fuzzy": allow minor differences (whitespace, punctuation)
}}

\item{flag_threshold}{What percentage difference triggers a flag?
Default 5 (flag differences >5\%).}

\item{auto_resolve}{Automatically resolve conflicts using priority rules?
Default TRUE. If FALSE, keeps all versions for user review.}

\item{auto_harmonize}{Apply intelligent auto-harmonization rules?
Default TRUE. When enabled, automatically fixes obvious errors like:
\itemize{
\item Missing values (backfill from available source)
\item Negative values for positive fields (use positive value)
\item Future dates for historical fields (use past date)
\item Rounding differences <1\% (use more precise value)
\item String normalization (whitespace, punctuation, case)
Set to FALSE to only use priority rules without smart harmonization.
}}
}
\value{
Invisibly returns the configuration list
}
\description{
Set rules for how conflicts between data sources should be resolved.
These settings persist for the R session.
}
\examples{
\dontrun{
# Prioritize CSV data (most official)
rb_discrepancy_config(source_priority = c("csv", "api", "epa"))

# Be more tolerant of numeric differences
rb_discrepancy_config(numeric_tolerance = 0.05)  # 5\% tolerance

# Don't auto-resolve - let user choose
rb_discrepancy_config(auto_resolve = FALSE)

# Disable smart harmonization (only use source priority)
rb_discrepancy_config(auto_harmonize = FALSE)

# Reset to defaults
rb_discrepancy_config("reset")
}
}
