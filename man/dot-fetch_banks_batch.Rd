% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-batch.R
\name{.fetch_banks_batch}
\alias{.fetch_banks_batch}
\title{Fetch Multiple Banks in Batches}
\usage{
.fetch_banks_batch(
  bank_ids,
  what = "all",
  batch_size = DEFAULT_CHUNK_SIZE,
  max_concurrent = DEFAULT_MAX_CONCURRENT,
  quietly = FALSE,
  ...
)
}
\arguments{
\item{bank_ids}{Character vector of bank IDs to fetch}

\item{what}{What data to fetch ("all", "spatial", "summary", etc.)}

\item{batch_size}{Number of banks to fetch per batch (default: from constants)}

\item{max_concurrent}{Maximum number of concurrent requests (default: from constants)}

\item{quietly}{If TRUE, suppress progress messages}

\item{...}{Additional parameters passed to rb_get()}
}
\value{
List of bank data, one element per bank
}
\description{
Fetches bank data for multiple IDs using batched requests instead of
sequential individual requests. This dramatically improves performance
when fetching many banks.
}
\details{
Instead of making N individual API calls for N banks (which takes N * delay time),
this function batches the requests and optionally makes them concurrently,
reducing total time significantly.

Performance comparison:
\itemize{
\item Sequential (old): 100 banks * 0.2s = 20 seconds
\item Batched (new): (100 banks / 10 per batch) * 0.2s = 2 seconds
\item Concurrent batched: Even faster with parallel requests
}
}
\examples{
\dontrun{
# Fetch 50 banks efficiently
bank_ids <- c("SAC-2020-001", "SAC-2020-002", ..., "SAC-2020-050")
results <- .fetch_banks_batch(bank_ids, what = "summary")
}
}
\keyword{internal}
