% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{banks_lookup}
\alias{banks_lookup}
\title{Bank ID to Name Lookup Table}
\format{
A tibble with 4,938 rows and 7 columns:
\describe{
\item{name}{Bank name (character)}
\item{bank_id}{Numeric bank identifier (integer)}
\item{state}{State(s) where bank operates (character)}
\item{district}{USACE district (character)}
\item{year_established}{Year the bank was established (integer)}
\item{source}{Data source: "epa" (EPA ArcGIS) or "api" (RIBITS API) (character)}
\item{name_normalized}{Normalized name for fuzzy matching (character)}
}
}
\source{
Generated from:
\itemize{
\item EPA ArcGIS MapServer (https://geopub.epa.gov/arcgis/rest/services/NEPAssist/RIBITS/MapServer)
\item RIBITS API (https://ribits.ops.usace.army.mil/ords/RI/public/bank_site_list/)
}
}
\usage{
banks_lookup
}
\description{
A bundled lookup table mapping bank IDs to bank names and metadata.
This dataset is generated from EPA ArcGIS and RIBITS API sources and
shipped with the package to provide offline fallback and faster lookups.
}
\details{
This dataset is used internally by \code{\link[=rb_build_name_lookup]{rb_build_name_lookup()}} and \code{\link[=rb_match_names]{rb_match_names()}}
to provide fast name-to-ID matching without requiring API calls.

The lookup table is built from three sources in priority order:
\enumerate{
\item EPA ArcGIS (highest priority, most reliable)
\item RIBITS API (real-time data)
\item Transactions by Watershed CSV (optional)
}
\subsection{Updating}{

This data is updated periodically before package releases. To generate
a fresh version, run:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{source("data-raw/banks_lookup.R")
}\if{html}{\out{</div>}}

The bundled data includes a \code{generated_date} attribute showing when it was created.
}
}
\section{Automatic Updates}{


When you call \code{\link[=rb_build_name_lookup]{rb_build_name_lookup()}}, it will:
\itemize{
\item Check a persistent user cache first (refreshed every 30 days by default)
\item If the cache is stale or missing, fetch fresh data from APIs
\item Use this bundled data as a fallback if APIs are unavailable
}

This ensures you get up-to-date bank information even if the package hasn't
been updated recently, while still providing offline functionality.
}

\examples{
# Load the bundled lookup data
data(banks_lookup)

# View metadata
attr(banks_lookup, "generated_date")
attr(banks_lookup, "n_banks")

# Summary by source
table(banks_lookup$source)

# Find a specific bank
banks_lookup |>
  dplyr::filter(grepl("Wetland", name, ignore.case = TRUE)) |>
  head()
}
\keyword{datasets}
